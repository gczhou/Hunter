use super::Transaction;

pub struct Commands;

impl Commands {
    pub fn send_transaction(tx: String) {
        //let mut transaction: SignedTransaction;
        if tx == "" {
            //
        } else {
            //
        }
    }
}
